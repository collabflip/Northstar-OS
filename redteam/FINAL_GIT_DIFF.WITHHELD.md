# FINAL_GIT_DIFF.txt — withheld from public delivery

The final git diff is withheld from the public delivery: it quotes pre-decontamination identifiers in removed-line context. It is retained in the internal archive (`internal/`). The diff summary (68 files, +3,458/−543) and every fix are described in `11_PATCHES.md` and the per-area reports.
